# 用户与业务资源

本项目以本地用户作为业务资源的所有者。以下用语同时用于资源模型和账户体系。

## 领域用语

**本地用户（User）**：
本项目认可的独立业务主体；同一个用户可以拥有多种登录方式，其资源归属不随登录方式变化。
避免：将邮箱、第三方账号或某一种凭据称为用户本身。

**登录账号（Account）**：
本地用户关联的一种账号身份，包括本地密码账号和第三方提供商账号；一个用户可以关联多个登录账号。
避免：把登录账号当作资源所有者，或与本地用户混用。

**登录方式（LoginMethod）**：
绑定到一个本地用户、可用于证明该用户身份的登录入口；当前包括密码和 GitHub OAuth 登录，其他提供商需新增适配。
避免：登录方式与登录会话混用。

**本地凭据（Credential）**：
本项目登记并验证的身份凭据；当前为本地密码账号的密码凭据。
避免：将本项目的会话令牌或第三方 API 授权称为本地凭据。

**第三方身份（ExternalIdentity）**：
外部身份提供方确认的独立主体；只有与本地用户建立绑定后，才成为该用户的登录方式。
避免：用第三方邮箱或昵称代替第三方身份。

**联系邮箱（Email）**：
用户在本项目登记的联系地址；经过本项目确认后，可按账户策略用于密码登录、通知或恢复。
避免：将外部提供方返回的邮箱直接视为已登记的联系邮箱。

**账号绑定（AccountLink）**：
在证明双方控制权后，将第三方身份关联为指定本地用户的登录账号。
避免：自动按同邮箱合并用户。

**用户合并（UserMerge）**：
在证明两个本地用户的控制权后，将其登录方式和业务归属按明确规则归集到一个本地用户。
避免：与账号绑定或简单的凭据转移混用。

**登录会话（Session）**：
一次成功登录建立的持续访问资格，可以独立过期或撤销。
避免：将会话续期称为重新登录。

**会话令牌（SessionToken）**：
客户端证明其持有某个登录会话的随机秘密；用户身份和有效期由服务器记录决定。
避免：将公开的会话记录 ID 当作会话令牌，或认为 Bearer 必须使用 JWT。

**重新认证（Reauthentication）**：
用户为一项敏感操作再次证明其对已绑定登录方式的控制权。
避免：将持有会话或延长会话期限视为重新认证。

**审计事件（AuditEvent）**：
本项目对用户、系统或匿名访问者的一次重要操作及其结果的记录，明确操作者、目标资源和资源归属；当前记录认证和账号操作，其他业务可追加同一表；项目、任务 CRUD 尚未接入。
避免：与待发送通知、运行错误日志或待执行任务混用。
